.git
.github
.venv
.env
.mypy_cache
.pytest_cache
.ruff_cache
.coverage
htmlcov
**/__pycache__
*.py[cod]
docs
# README.md is referenced by pyproject's `readme` field, so the package build
# needs it inside the image. Do not ignore it.
